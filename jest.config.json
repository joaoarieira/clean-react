{
  "roots": ["<rootDir>/src"],
  "preset": "ts-jest",
  "testEnvironment": "jsdom",
  "collectCoverageFrom": [
    "<rootDir>/src/**/*.{ts,tsx}"
  ],
  "coverageDirectory": "coverage",
  "transform": {
    ".+\\.(ts|tsx)$": "ts-jest"
  },
  "moduleNameMapper": {
    "\\.(css|scss)$": "identity-obj-proxy",
    "@/(.*)": "<rootDir>/src/$1"
  }
}